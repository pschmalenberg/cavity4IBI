# Main Figure 3

## Status

Complete retained MATLAB plot source and numerical data.

## Files

- `plot_cav.m`: plots measured HBK sweeps, COMSOL points, and circuit-model resonance lines.
- `cavity_data.mat`: `HBKMeasurement` and `ModelData` arrays used by the script.

Run `plot_cav.m` in MATLAB with this directory as the working directory.
Original release paths: `figures/plot_cav.m` and `figures/cavity_data.mat`.